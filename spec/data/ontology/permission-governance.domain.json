{
  "name": "permission-governance",
  "description": "Claude permission routing in which PreToolUse records observations without deciding, Notification proves a human prompt, and fixed keystrokes relay remote answers.",
  "presetRules": [],
  "templateRules": [],
  "membership": [
    {
      "pathPattern": "(^|/)src/(notification-hook|permission-(hook|pending|notify|runtime|answer|audit|audit-report|rules|evasion|log))\\.ts$"
    },
    {
      "pathPattern": "(^|/)tests/permission-(hook|proxy|runtime|notification|rules)\\.test\\.ts$"
    }
  ],
  "cardTemplate": "Summarise the permission-governance domain: Claude remains authoritative in every permission mode; Lictor records PreToolUse observations, raises cards only from permission Notifications, and relays fixed TUI answers."
}
